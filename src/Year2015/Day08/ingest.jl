"""
    input(path)

For this puzzle, we just need to read in the lines of the file to strings, 
for which there is a convenient function in the standard library to do just that.
"""
ingest(path) = readlines(path)
